language: c
os: linux
dist: bionic
sudo: false
addons:
  apt:
    packages:
      - emacs
      - gnutls-bin
install:
  - ln -sf $PWD $HOME/.emacs.d
script:
  - |
    emacs \
      --debug-init \
      --batch \
      --eval='(load-file "$HOME/.emacs.d/init.el")' \
      --eval='(magit-status)' \
      --kill
